[project]
name = "server"
version = "0.1.0"
description = "知识库系统后端"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "llm",
    "api",
    "agent",
    "kb",
    "parser",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
    "pytest-asyncio>=1.3.0",
]
