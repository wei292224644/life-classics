[project]
name = "llm"
version = "0.1.0"
dependencies = [
    "dashscope>=1.17.0",
    "ollama>=0.6.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["llm"]
