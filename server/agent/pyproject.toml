[project]
name = "agent"
version = "0.1.0"
dependencies = [
    "llm = { path = "../llm" }",
    "kb = { path = "../kb" }",
    "parser = { path = "../parser" }",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
