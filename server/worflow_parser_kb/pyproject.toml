[project]
name = "worflow_parser_kb"
version = "0.1.0"
dependencies = [
    "llm",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["worflow_parser_kb"]

[tool.uv.sources]
llm = { workspace = true }
