# UniApp 首页及搜索功能设计（Phase 1）

## 1. 概述

本设计针对"食品安全助手"UniApp 小程序的第一阶段，明确首页定位、搜索能力及底部 Tab 结构。

**核心业务链路：** 用户扫描条形码 → 进入产品详情（如数据库有）→ 点击配料进入配料详情。

**Phase 1 交付范围：**
- 极简扫码体验
- 食品/配料通用搜索
- 最近扫描记录（本地存储）
- 底部 Tab 框架预留

**后续扩展预留：**
- AI 助手 Tab（限流/付费）
- 广告 Banner 位
- 家庭成员档案
- 账号系统

---

## 2. 底部 Tab 结构

| Tab | 页面路径 | 功能 |
|-----|----------|------|
| 首页 | `/pages/index` | 扫码 + 最近扫描 |
| 搜索 | `/pages/search` | 食品/配料搜索 → 结果列表 |
| 我的 | `/pages/profile` | 框架占位（后续扩展） |

**Phase 1 不设 AI Tab** — AI 能力从产品详情页/配料详情页底部栏触发，避免用户过度使用付费能力。

---

## 3. 首页（`/pages/index`）

### 页面布局

```
┌──────────────────────────────────┐
│  🍎 食品安全助手                  │
│  扫码查配料 · 了解你吃的每一口      │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐ │
│  │      📷  扫一扫             │ │  ← 主 CTA
│  └────────────────────────────┘ │
│                                  │
│  ┌────────────────────────────┐ │
│  │  🔍  搜索食品或配料          │ │  ← 跳转搜索页
│  └────────────────────────────┘ │
│                                  │
│  ──── 最近扫描 ────              │
│  ┌────────────────────────────┐ │
│  │ 🥫 午餐肉        2分钟前   │ │
│  ├────────────────────────────┤ │
│  │ 🍝 方便面        昨天       │ │
│  └────────────────────────────┘ │
│                                  │
└──────────────────────────────────┘
┌──────────────────────────────────┐
│  首页        搜索        我的     │
└──────────────────────────────────┘
```

### 交互说明

| 元素 | 行为 |
|------|------|
| 扫一扫按钮 | 调用系统扫码，成功后跳转 `/pages/product?barcode=xxx` |
| 搜索框 | 点击跳转 `/pages/search` |
| 最近扫描项 | 点击跳转 `/pages/product?barcode=xxx` |

**最近扫描存储：** 使用 `uni.setStorageSync('recent_scans', [])` 本地存储，最多保留 20 条，按时间倒序。

---

## 4. 搜索页（`/pages/search`）

### 页面布局

```
┌──────────────────────────────────┐
│  🔍 搜索食品或配料                │
│  ┌────────────────────────────┐ │
│  │  输入框 + 清空按钮           │ │
│  └────────────────────────────┘ │
├──────────────────────────────────┤
│  搜索历史                        │
│  苯甲酸钠  香兰素  方便面         │
├──────────────────────────────────┤
│  搜索结果（输入后显示）            │
│  ┌────────────────────────────┐ │
│  │ 🏷️ 配料  苯甲酸钠          │ │  ← 类型标签
│  │ 食品添加剂 · 防腐剂          │ │
│  ├────────────────────────────┤ │
│  │ 🥫 食品  康师傅方便面        │ │
│  │ 方便食品 · 高盐              │ │
│  └────────────────────────────┘ │
└──────────────────────────────────┘
```

### 搜索逻辑

```
用户输入关键词
       │
       ▼
   调用后端搜索接口
       │
  ┌────┴────┐
  │         │
  ▼         ▼
食品结果   配料结果
列表       列表
  │         │
  │    ┌────┴────┐
  ▼    ▼         ▼
产品详情 配料详情 配料详情
(/product) (/ingredient-detail)
```

**判断规则：** 后端根据关键词返回数据类型，前端根据 `type` 字段决定跳转：
- `type: 'product'` → `/pages/product?barcode=xxx`
- `type: 'ingredient'` → `/pages/ingredient?ingredientId=xxx`

**搜索历史：** 本地存储，最多 10 条，支持一键清空。

---

## 5. 新增/调整页面清单

| 页面 | 操作 | 说明 |
|------|------|------|
| `/pages/index` | 调整 | 添加搜索入口 + 最近扫描 |
| `/pages/search` | 新建 | 通用搜索页 |
| `/pages/profile` | 新建 | Tab 框架占位（UI 先做，内容后续） |
| `/pages/product` | 不变 | 已有功能 |
| `/pages/ingredient` | 不变 | 已有功能 |

---

## 6. 路由跳转汇总

| 来源 | 目标 | 参数 |
|------|------|------|
| 首页扫码 | `/pages/product` | `barcode` |
| 首页搜索入口 | `/pages/search` | — |
| 搜索结果-食品 | `/pages/product` | `barcode` |
| 搜索结果-配料 | `/pages/ingredient` | `ingredientId` |
| 产品页点击配料 | `/pages/ingredient` | `ingredientId` |
| 配料页点击相关产品 | `/pages/product` | `barcode` |
| 首页最近扫描 | `/pages/product` | `barcode` |

---

## 7. 数据存储

| 数据 | 存储方式 | 键名 | 限制 |
|------|----------|------|------|
| 最近扫描 | `uni.setStorageSync` | `recent_scans` | 最多 20 条 |
| 搜索历史 | `uni.setStorageSync` | `search_history` | 最多 10 条 |

---

## 8. 后续扩展预留

| 预留点 | 说明 |
|--------|------|
| AI Tab | 独立 Tab，限未登录用户使用次数，后续接入付费 |
| 广告 Banner | 首页搜索框下方，预留 120rpx 高度 |
| 家庭成员档案 | 我的页扩展，支持创建家庭成员 Profile |
| 账号系统 | 支持登录/注册，数据同步到服务端 |
